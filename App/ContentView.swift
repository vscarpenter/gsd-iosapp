import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("GSD")
            .font(.system(.largeTitle, design: .serif))
            .padding()
    }
}

#Preview { ContentView() }
